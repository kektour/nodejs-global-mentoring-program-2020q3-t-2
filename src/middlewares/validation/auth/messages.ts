export enum LoginMessage {
  Required = 'Name field is required',
}

export enum PasswordMessage {
  Required = 'Password field is required',
}

export enum RefreshTokenMessage {
  Required = 'RefreshToken field is required',
}
