export enum NameMessage {
  Required = 'Name field is required',
}

export enum PermissionsMessage {
  Required = 'Permissions field is required',
  Includes = 'Permissions items do not match any of the allowed types',
}
